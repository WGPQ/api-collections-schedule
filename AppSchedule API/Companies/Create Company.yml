name: Create Company
description: Crear empresa y asignar Owner al usuario autenticado.
request:
  method: POST
  path: /api/companies
  headers:
    Authorization: Bearer {{accessToken}}
    Content-Type: application/json
  body:
    name: Mi Empresa
responses:
  success: 201
