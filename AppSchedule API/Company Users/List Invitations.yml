name: List Invitations
description: Obtener invitaciones de la empresa activa.
request:
  method: GET
  path: /api/company-users/invitations
  headers:
    Authorization: Bearer {{accessToken}}
    X-Company-Id: "{{companyId}}"
responses:
  success: 200
