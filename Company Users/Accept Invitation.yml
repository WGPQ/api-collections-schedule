info:
  name: Accept Invitation
  type: http
  seq: 5

http:
  method: POST
  url: '{{baseUrl}}/api/company-invitations/{{invitationToken}}/accept'
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
