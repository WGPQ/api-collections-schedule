info:
  name: Day Agenda
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/api/calendar/agenda/day'
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
    - name: X-Company-Id
      value: '{{companyId}}'
  params:
    - name: date
      value: '2026-05-24'
      type: query
    - name: responsibleUserId
      value: '{{responsibleUserId}}'
      type: query
      disabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
