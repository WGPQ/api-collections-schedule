info:
  name: List Availability Rules
  type: http
  seq: 11

http:
  method: GET
  url: '{{baseUrl}}/api/calendar/availability-rules'
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
    - name: X-Company-Id
      value: '{{companyId}}'
  params:
    - name: page
      value: '1'
      type: query
    - name: pageSize
      value: '20'
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Lista reglas de disponibilidad de la empresa activa.

  Respuesta paginada con `items` y `meta`.
