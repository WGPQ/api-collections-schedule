name: Cancel Event
description: Cancelar evento de agenda.
request:
  method: POST
  path: /api/calendar/events/{{appointmentId}}/cancel
  headers:
    Authorization: Bearer {{accessToken}}
    X-Company-Id: "{{companyId}}"
responses:
  success: 204
