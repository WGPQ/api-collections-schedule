name: List Availability Rules
description: Obtener reglas de disponibilidad.
request:
  method: GET
  path: /api/calendar/availability-rules
  headers:
    Authorization: Bearer {{accessToken}}
    X-Company-Id: "{{companyId}}"
responses:
  success: 200
